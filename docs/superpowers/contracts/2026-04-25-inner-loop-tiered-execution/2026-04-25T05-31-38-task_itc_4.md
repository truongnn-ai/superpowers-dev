# Task ITC 4: Add tier-aware dispatch to subagent-driven-development SKILL.md

**Task:** Modify `skills/subagent-driven-development/SKILL.md` — insert ## Tier-Aware Dispatch section
**Negotiated:** 2026-04-25

## Contract

```yaml
test_contract:
  tiers_required: [unit]
  test_commands:
    - command: |
        grep -q '^## Tier-Aware Dispatch' skills/subagent-driven-development/SKILL.md &&
        grep -q 'fail-safe.*heavy' skills/subagent-driven-development/SKILL.md &&
        grep -q '^### Trivial flow' skills/subagent-driven-development/SKILL.md &&
        grep -q '^### Standard flow' skills/subagent-driven-development/SKILL.md &&
        grep -q '^### Heavy flow' skills/subagent-driven-development/SKILL.md &&
        echo OK
      tier: unit
  must_cover:
    - Section ## Tier-Aware Dispatch inserted before ## ITC Negotiation
    - Fail-safe rule (no tier block → heavy) documented
    - Trivial/Standard/Heavy per-tier flows documented
    - Dispatch pseudocode present
coding_agent: ✅
testing_agent: ✅
```
