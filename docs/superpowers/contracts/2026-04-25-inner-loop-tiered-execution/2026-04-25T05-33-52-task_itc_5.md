# Task ITC 5: Add escalation handling and end-of-run ledger

**Task:** Modify `skills/subagent-driven-development/SKILL.md` — insert ## Escalation Handling section, update Handling Implementer Status, update Red Flags
**Negotiated:** 2026-04-25

## Contract

```yaml
test_contract:
  tiers_required: [unit]
  test_commands:
    - command: |
        grep -q '^## Escalation Handling' skills/subagent-driven-development/SKILL.md &&
        grep -q 'fresh re-dispatch' skills/subagent-driven-development/SKILL.md &&
        grep -q 'one-way only' skills/subagent-driven-development/SKILL.md &&
        grep -q 'escalation_count' skills/subagent-driven-development/SKILL.md &&
        grep -q '## Escalations' skills/subagent-driven-development/SKILL.md &&
        grep -q 'five statuses' skills/subagent-driven-development/SKILL.md &&
        grep -q 'ESCALATE:' skills/subagent-driven-development/SKILL.md &&
        echo OK
      tier: unit
  must_cover:
    - Section ## Escalation Handling inserted after ## Tier-Aware Dispatch and before ## ITC Negotiation
    - fresh re-dispatch language present
    - one-way only rule documented
    - escalation_count field referenced
    - ## Escalations ledger format documented
    - Handling Implementer Status updated to five statuses
    - ESCALATE status block added
    - Red Flags updated with two new bullets
coding_agent: ✅
testing_agent: ✅
```
