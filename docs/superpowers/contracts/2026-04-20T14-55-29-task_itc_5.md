```yaml
task_id: 5
task_name: "Rewrite testing-agent solution-ITC review block for coverage matrix"

implementation_scope:
  components:
    - "skills/subagent-driven-development/testing-agent-prompt.md (solution-ITC review block rewrite)"
  integration_surfaces: []

test_contract:
  tiers_required: [unit]
  rationale: "Markdown edit verified by grep for all 8 spec requirements"

  unit:
    commands:
      - "grep -q 'journeys\\.yaml' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'testing-strategies\\.md' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'coverage matrix' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'returns 200' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'element visible' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'works correctly' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'behaves as expected' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -qE '(too hard|not critical)' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'one-word' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'runnable' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'required_services' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'full_suite' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'Journeys.yaml (paste in full)' skills/subagent-driven-development/testing-agent-prompt.md"
      - "grep -q 'Testing Strategies Playbook (paste in full)' skills/subagent-driven-development/testing-agent-prompt.md"
    must_cover:
      - "journeys.yaml listed as required input"
      - "testing-strategies.md listed as required input"
      - "Matrix shape completeness check present"
      - "Shallow assertion_shape rejection: returns 200"
      - "Shallow assertion_shape rejection: element visible"
      - "Shallow assertion_shape rejection: works correctly"
      - "Shallow assertion_shape rejection: behaves as expected"
      - "Lazy na rejection: too hard / not critical"
      - "Lazy na rejection: one-word skip"
      - "Commands must be runnable"
      - "required_services completeness check"
      - "full_suite command check"
      - "Journeys.yaml paste-in section"
      - "Testing Strategies Playbook paste-in section"

acceptance_criteria:
  unit: all pass
  no_regression: true

sign_off:
  coding_agent: ✅
  testing_agent: ✅
```
